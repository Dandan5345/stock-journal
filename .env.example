# Database configuration
# When running via `docker compose up`, set DATABASE_HOST=postgres so the
# api container can resolve the database by its service name.
DATABASE_HOST=localhost
DATABASE_PORT=5432
DATABASE_NAME=stock_journal
DATABASE_USER=stock_journal_user
DATABASE_PASSWORD=change_me
